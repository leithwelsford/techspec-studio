# Environment Variables for TechSpec Studio
# Copy this file to .env.local and fill in your values
# .env.local is Git-ignored and won't be committed

# ========================================
# OpenRouter API Configuration
# ========================================

# OpenRouter API Key (REQUIRED)
# Get your API key from: https://openrouter.ai/keys
# This key will be used as a fallback if no key is stored in the browser
VITE_OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Default AI Model (OPTIONAL)
# Examples:
#   - anthropic/claude-3.5-sonnet (recommended for technical specs)
#   - anthropic/claude-3-opus (best quality, slower)
#   - openai/gpt-4-turbo (good alternative)
#   - openai/gpt-5.1 (reasoning model, good for cascade refinement)
VITE_OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Temperature (OPTIONAL)
# Range: 0.0 (deterministic) to 2.0 (very creative)
# Recommended: 0.3-0.7 for technical documentation
VITE_AI_TEMPERATURE=0.7

# Max Tokens (OPTIONAL)
# Maximum tokens in AI response (higher = longer responses)
# Recommended: 4096-8192 for technical specs
VITE_AI_MAX_TOKENS=4096

# Enable Streaming (OPTIONAL)
# true = See AI responses as they're generated (faster perceived response)
# false = Wait for complete response (better for debugging)
VITE_AI_ENABLE_STREAMING=true

# ========================================
# Why use environment variables?
# ========================================
# - Settings persist across browser cache clears
# - No device fingerprint dependency (works across devices/browsers)
# - No need to re-enter settings every time
# - Safer than committing to git (since .env.local is ignored)
# - Easy to share config with team (copy .env.local.example)

# ========================================
# How to use:
# ========================================
# 1. Copy this file: cp .env.local.example .env.local
# 2. Edit .env.local and add your actual values
# 3. Restart the dev server: npm run dev
# 4. The app will automatically load settings from the environment
# 5. You can still override settings via the UI (AI Settings panel)
