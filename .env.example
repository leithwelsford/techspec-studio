# TechSpec Studio Environment Configuration
# Copy this file to .env.local and configure for your environment

# ============================================================================
# AI Provider Configuration (OpenRouter)
# ============================================================================

# OpenRouter API Key (get from openrouter.ai)
# VITE_OPENROUTER_API_KEY=sk-or-v1-...

# Default AI model to use
# VITE_OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# AI generation settings
# VITE_AI_TEMPERATURE=0.7
# VITE_AI_MAX_TOKENS=4000
# VITE_AI_ENABLE_STREAMING=true

# ============================================================================
# Pandoc Export Service Configuration
# ============================================================================

# URL of the Pandoc backend service
# Default: http://localhost:3001/api (development)
# Production: Set to your deployed backend URL
VITE_PANDOC_API_URL=http://localhost:3001/api

# ============================================================================
# Notes
# ============================================================================

# All variables are optional and have sensible defaults
# Only uncomment and set values if you need to override defaults
# Never commit .env.local to git (it's in .gitignore)
